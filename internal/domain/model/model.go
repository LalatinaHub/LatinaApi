package model

import (
	"github.com/LalatinaHub/common/model"
)

// Re-export core domain models from github.com/LalatinaHub/common/model
type (
	User      = model.User
	Server    = model.Server
	ProxyNode = model.ProxyNode
	KeyValue  = model.KeyValue
)
